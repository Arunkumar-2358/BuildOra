{
  "name": "buildora",
  "version": "1.0.0",
  "private": true,
  "description": "Buildora full-stack MERN marketplace for construction and interior projects.",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "server": "npm run server --prefix backend",
    "client": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
